/**
 * Validates all content in src/content/ against the Zod schemas.
 * Implemented in phase 1. For now it is a no-op so CI can already call it.
 */
console.log('validate:content: nothing to validate yet (phase 1)')
